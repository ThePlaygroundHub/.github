name: PlaygroundHub Organization Template

on:
  push:
    branches: [ $default-branch ] 

jobs:
  greeting:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Run a one-line script
        run: echo Hello from PlaygroundHub Organization
